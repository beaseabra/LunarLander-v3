# Dependências principais para o projeto de Reinforcement Learning
gymnasium[box2d]>=0.29.1  # Ambiente (com Box2D para LunarLander)
stable-baselines3[extra]>=2.2.1  # Algoritmos RL (PPO, A2C, DQN)
                                # [extra] inclui dependências como torch
                                # para garantir a compatibilidade com GPU/CPU

# Dependências para manipulação de dados e visualização
numpy>=1.24.0      # Computação numérica
pandas>=2.0.0      # Manipulação de dados (para logs e tabelas)
matplotlib>=3.7.0  # Geração de gráficos (curvas de recompensa)
seaborn>=0.12.0    # Geração de gráficos estatísticos (comparação de benchmarks)

# Dependências para geração de vídeos/GIFs (Secção 2.6 e 6.5)
imageio>=2.34.0    # Geração de GIFs a partir dos frames de renderização

# Dependência opcional para integração com logs (TensorBoard)
# stable-baselines3[extra] já deve incluir o tensorboard.
# tensorboard>=2.15.1